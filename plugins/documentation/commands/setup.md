---
description: Verify the Relay Documentation plugin is ready to answer from public Relay docs.
---

Call the `documentation_status` MCP tool. If the corpus is available, tell the user the Relay Documentation plugin is ready and mention that it answers only from public Relay documentation with source links. If the corpus is stale or using the bundled snapshot, say that plainly.
